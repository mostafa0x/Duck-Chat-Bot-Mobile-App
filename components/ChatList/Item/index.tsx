import React, { memo } from "react";
import { StyleSheet, Text, View } from "react-native";

function Item_ChatList() {
  return (
    <View>
      <Text>Item_ChatList</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default memo(Item_ChatList);
